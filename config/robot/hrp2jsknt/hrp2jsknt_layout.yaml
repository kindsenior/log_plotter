extend:
  - hrp2jsknt_joint_angle_layout.yaml
  - ../../st_layout.yaml
  - ../../force_sensors_layout.yaml
  - ../../imu_layout.yaml

main:
  tracking:
    legends:
      - {key: rh_st_diff, id: [0-5] }

  comp:
    legends:
      - { key: comp, id: [0-5] }

  # watt:
  #   legends:
  #     - { key: watt, id: [0-5] }
